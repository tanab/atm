#pragma once

///
/// \brief The ItemTypes enum defines types of an item being prefix, stem or suffix
///
enum class ItemTypes { PREFIX, STEM, SUFFIX };

///
/// \brief The Rules enum defines different rule classes for matching different solutions.
///
enum class Rules { AA, AB, AC, BC, CC };

